/* ==  คือเช็คแค่รูปร่างเหมือนกัน ไม่เช็ค Datatype
   === คือเช็คทั้งรูปร่าง ทั้ง DataType
*/

const x = 10;
if(x === 10){
    console.log('x is 10');
}else{
    console.log('x is not 10');
}

const color = x>10? 'red' : 'blue';
console.log(color)